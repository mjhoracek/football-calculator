import React from 'react'

const spreadTable = [[0.5, 50], [1, 60]];

const OddsSelector = ( {setOdds, handleOdds }) => {
const [inputType, setInputType] = useState("percentage")

const lookupSpread = (userInput) => {
    const spreadPercentage = spreadTable.filter()
}

if(percentage)
    return (
        <div>
            
        </div>
    )
}

export default OddsSelector
